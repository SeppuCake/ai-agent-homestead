# Agent Homestead

A lightweight pixel-room dashboard for learning TypeScript, Vite, and Three.js while visualizing AI-agent activity.

## Commands

```powershell
npm.cmd run dev
npm.cmd run build
```

Open `agent-homestead.code-workspace` in Visual Studio Code to use the included workspace settings.
